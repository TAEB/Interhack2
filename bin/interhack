#!/usr/bin/perl
use strict;
use warnings;
use lib 'lib';
use Interhack;
use Term::ReadKey;

$| = 1;
sub cont {
    ReadMode 0;
    ReadMode 3;
    # XXX: redraw the screen here...
    $SIG{CONT} = \&cont;
}
$SIG{CONT} = \&cont;
ReadMode 3;
END { ReadMode 0 }

Interhack->new->run;

